$('#places')
	.trigger('beforeupdate')
	.html('<%= escape_javascript(render :partial => "places/places") %>')
	.trigger('update');
notice('You are no longer voted for "<%= escape_javascript(@vote.place.name) %>".');