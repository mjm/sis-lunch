<% if @vote.valid? %>
$('#places').html("<%= escape_javascript(render :partial => "places/places") %>").trigger('update');
notice('You have voted for "<%= escape_javascript(@vote.place.name) %>".');
<% else %>
error('Your vote could not be placed.');
<% end %>