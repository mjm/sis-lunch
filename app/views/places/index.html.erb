<div id="add-buttons">
	<button id="add-place">Add a Place</button>

	<div id="add-place-form" style="display:none" class="add-form">
	<%= render :partial => 'place', :locals => {:place => @place} %>
	</div>
</div>

<table id="places"><%= render :partial => 'places' %></table>